{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug API tests",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${workspaceFolder}/pic"
        }
    ]
}